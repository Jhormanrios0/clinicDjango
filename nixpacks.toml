[phases.setup]
commands = [
  "apt-get update && apt-get install -y python3-pip python3-dev build-essential libpq-dev",
  "pip3 install django",
  "pip3 install psycopg2",
]

[phases.start]
commands = [
  "pip3 install gunicorn",
  "gunicorn clinicaTendencias.wsgi:application --bind 0.0.0.0:$PORT --workers 3"
]

